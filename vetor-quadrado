NICOLY FERREIRA 
Ler um conjunto de números reais, armazenando-o em vetor e calcular o quadrado
das componentes deste vetor, armazenando o resultado em outro vetor. Os
conjuntos têm 10 elementos cada. Imprimir todos os conjuntos (vetores).

*******************************************************************************/
#include <stdio.h>

int main()
{
    int main() {
    float conjunto[10];
    float quadrados[10];
    int i;

    // Lendo o conjunto de números reais
    printf("Digite 10 números reais:\n");
    for (i = 0; i < 10; i++) {
        scanf("%f", &conjunto[i]);
    }

    // Calculando o quadrado das componentes do vetor
    for (i = 0; i < 10; i++) {
        quadrados[i] = conjunto[i] * conjunto[i];
    }

    // Imprimindo os dois conjuntos (vetores)
    printf("Conjunto lido:\n");
    for (i = 0; i < 10; i++) {
        printf("%.2f ", conjunto[i]);
    }
    printf("\n");

    printf("Quadrados das componentes:\n");
    for (i = 0; i < 10; i++) {
        printf("%.2f ", quadrados[i]);
    }
    printf("\n");

 
    return 0;
}
